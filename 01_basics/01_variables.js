const accountId = 1234567
let accountName = "Mayur Rajput"
var accountAmmount = "1234"


// accountId = 2 // Not allowed // beacause const are not change onces it declared 

// we can change the let keyword value but not for const

// Preferable to use let instead of var beacuse of the scope of the variable and functional scope of the variable


console.log(accountId);
console.table([accountId,accountAmmount,accountName]);